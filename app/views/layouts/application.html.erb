<!DOCTYPE html>
<html>
<head>
  <title>Landlord</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>

<%= yield %>

<div id="notaspie">
  <pre>
  <%#
    String.new.tap do |ivs|
      instance_variables.each do |v|
        ivs << "#{v}: #{ instance_variable_get(v) }\n"
      end
    end
  %>
  </pre>
</div>
</body>
</html>
